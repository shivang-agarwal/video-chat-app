{
  "name": "video-app",
  "version": "1.0.0",
  "description": "This app will enable the users to do peer to peer video call",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"ts-node src/index.ts",
    "dev":"nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts"
  },
  "author": "Shivang Agarwal",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.3",
    "@types/socket.io": "^2.1.4",
    "nodemon": "^2.0.2",
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  }
}
